const GradientText = () => {
  return (
    <div className="flex min-h-screen w-screen flex-col items-center p-24">
      <span className={""}>from hello world to IPO.</span>
      <span className="">Shopify</span>
    </div>
  );
};

export default GradientText;
